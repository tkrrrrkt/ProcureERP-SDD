{
  "name": "procure-erp-sdd",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "pnpm --filter @procure/api run start:dev",
    "dev:bff": "pnpm --filter @procure/bff run start:dev",
    "dev:web": "pnpm --filter @procure/web run dev",
    "build:api": "pnpm --filter @procure/api run build",
    "build:bff": "pnpm --filter @procure/bff run build",
    "build:web": "pnpm --filter @procure/web run build"
  },
  "devDependencies": {
    "dotenv": "^17.2.3",
    "tsx": "^4.21.0",
    "v0-sdk": "^0.15.3"
  }
}
